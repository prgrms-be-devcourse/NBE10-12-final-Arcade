package com.back.domain.party.assemble.repository;

import com.back.domain.party.assemble.entity.PartyAssemble;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PartyAssembleRepository extends JpaRepository<PartyAssemble, Long> {
}
